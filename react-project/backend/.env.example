PORT=4000

# Optional — if unset, the server still saves submissions to backend/data/submissions.json
# and simply skips sending an email notification.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_NOTIFY_TO=

# Optional — required to view saved submissions via GET /api/contact/submissions
ADMIN_TOKEN=
